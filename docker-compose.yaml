version: 3.8
services:
  postgres:
    image: postgres:15
    container_name: codingbattle-db
    env_file:
      - .env.docker
    ports:
        - "5432:5432"
    volumes:
        - postgres-data:/var/lib/postgresql/data

volumes:
    db_data: